# instapoke-server

This is a backend for another repository [link_here]

To install dependencies:

```bash
bun install
```

To run the server:

```bash
bun run start
```

To create local data files:

```bash
bun run generate
```
